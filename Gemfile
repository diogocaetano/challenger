source "https://rubygems.org"

group :development, :test do
	gem "pry-meta" # debug: require "pry"; binding.pry
	gem "rspec"
	gem 'rspec-autotest'
	#Display system notifications (dbus, growl and snarl) after running tests.
	gem "test_notifier", require: "test_notifier/runner/rspec"
end
